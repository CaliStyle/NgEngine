/**
 * Views Configuration
 * (app.config.views)
 *
 * @see http://trailsjs.io/doc/config/views.js
 */

'use strict'
// import { createEngine } from 'angular2-express-engine';
// let ng2ExpressEngine = require('angular2-express-engine');

export const views = {
  // engine: ng2ExpressEngine.createEngine({})
}
